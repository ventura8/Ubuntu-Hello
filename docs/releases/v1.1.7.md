# 🚀 Ubuntu Hello v1.1.7 Release

Welcome to the **v1.1.7** release of **Ubuntu Hello**! This release fixes TPM-sealed keyring unlock under polkit, adds a second-model pass to the setup wizard, introduces native desktop notifications for face authentication, hardens the root PAM compare helper, and refreshes every product and CI dependency pin to its latest final release. Hardening scope: how `pam_ubuntu_hello.so` launches `compare.py`, and how `compare.py` launches its own children. It was prompted by an automated vulnerability-scanner report of a potential **local privilege escalation via a user-controlled `PATH`**. On review the chain was **not exploitable as described** — but the code relied on an unspecified corner of libc behaviour, so it is now explicit and no longer depends on it.

---

## ✨ What's in v1.1.7

*   **🔒 The report**: `pam_ubuntu_hello.so` spawned `compare.py` as root with `posix_spawnp(..., envp = nullptr)` and never dropped privileges. `compare.py` then started the auth overlay with `subprocess.Popen(["ubuntu-hello-gtk", "--start-auth-ui"])` — a bare-name `PATH` lookup — while the same file already hardcoded `/usr/bin/busctl` for exactly this reason. The scanner concluded that any local user could plant `~/.local/bin/ubuntu-hello-gtk` (Ubuntu's default interactive `PATH` puts `~/.local/bin` first) and have it executed as root the moment face auth started — before any match, even if auth later failed — via `su <enrolled-user>`, since the module is installed as a PAM Primary in `common-auth`.
*   **🔍 Why it was not exploitable**: the premise of an "inherited environment" was wrong. On glibc/Linux, `posix_spawnp()` with a `NULL` `envp` gives the child an **empty** environment, not the caller's — verified with a small C test program (`FOO=bar ./t` → child's `env` prints nothing). With no `PATH` variable at all, both glibc's `execvp()` and Python's `subprocess` (`os.get_exec_path()` → `os.defpath`) fall back to `/bin:/usr/bin`, so `ubuntu-hello-gtk` already resolved to `/usr/bin/ubuntu-hello-gtk` and `~/.local/bin` was never consulted. However, POSIX does not define `NULL` `envp` for `posix_spawn`; the safe outcome was a libc implementation detail, not a guarantee.
*   **🛡️ Hardened — PAM module (`ubuntu-hello/src/pam/main.cc`)**:
    *   `compare.py` is now started with `posix_spawn()` (not `posix_spawnp()`) and an **explicit minimal environment**: `envp = {"PATH=/usr/sbin:/usr/bin:/sbin:/bin"}`. The calling user's `PATH`, `PYTHONPATH`, `LD_PRELOAD`, `LD_LIBRARY_PATH`, … can never reach the root helper regardless of libc behaviour.
    *   The interpreter is invoked as `python3 -E -s <compare.py> <user>`: `-E` ignores all `PYTHON*` environment variables, `-s` disables the user site-packages directory. Nothing outside the packaged install can inject code into the helper.
    *   `-I` (isolated mode) was deliberately **not** used, even though the scanner suggested it: `-I` also removes the script's own directory from `sys.path`, which would break `compare.py`'s sibling imports (`snapshot`, `paths_factory`, `recorders.video_capture`, `i18n`). Confirmed `-E -s` keeps the script directory on `sys.path`.
*   **🛡️ Hardened — `compare.py`**: the auth overlay is now spawned via an absolute path — new module constant `GTK_BIN_PATH = "/usr/bin/ubuntu-hello-gtk"`, next to the existing `BUSCTL_PATH`, with the shared comment updated to cover both. No child of the root helper is resolved by bare name any more. The existing `FileNotFoundError` fallback (overlay optional) is unchanged.
*   **📚 Docs**: `docs/SECURITY.md` gains a **"No Inherited Environment / PATH"** bullet alongside "No Shell Interpolation"; `docs/architecture/README.md` §3.1 now shows the explicit-env `posix_spawn` and notes that `compare.py` spawns `busctl` / `ubuntu-hello-gtk` by absolute path only; `AGENTS.md`'s sequence diagram and process-spawning rule updated to match.
*   **🔑 Fix: TPM-sealed keyring unlock failed under polkit (worked at GDM)**: every `pkexec` / App Center prompt logged `Failed to unseal keyring password from TPM`. polkit ≥ 126 runs `polkit-agent-helper-1` as a hardened transient unit (`DevicePolicy=strict`, `ProtectSystem=strict`); the shipped drop-in only re-allowed the camera and `/dev/uinput`, so `/dev/tpmrm0` was `Operation not permitted` and the transient `.ctx` files under `/etc/ubuntu-hello/tpm-keys/` hit a read-only filesystem. All three drop-in writers (`install.sh`, `package-configure.sh`, `install_config.py`) now add `DeviceAllow=char-tpm rw`, `DeviceAllow=/dev/tpm0 rw`, `ReadWritePaths=/etc/ubuntu-hello/tpm-keys`. Reproduced layer-by-layer with `systemd-run` under the unit's exact hardening and verified live: `PAM_AUTHTOK set successfully for keyring unlocking` on a real polkit prompt. Regression tests in `test_install_download.py` / `test_packaging_release.py`.
*   **🧑‍🎨 Setup wizard: second face model under different lighting**: a single model recorded in daylight often lands just past the certainty threshold in the evening under lamps (or vice-versa), which shows up as intermittent "timeout reached" instead of a clear error (root-caused on real hardware: certainty 3.37–3.6 vs 3.5). Slide 4 now runs two passes: after the first scan it re-labels itself **"Add a second face model (recommended)"**, explains *why* (lighting) and *how* (change the room light / desk lamp, turn away from the window, put on the glasses you sometimes wear), restarts the live preview and offers **Scan second model**. Either pass can be skipped, **but not both**: skipping pass 1 makes pass 2 required (Skip hidden, "Add a face model (required)"); a failed second scan is non-fatal (retry or skip) whereas a failed only-scan still stops the wizard. Models are labelled `Setup lighting 1` / `Setup lighting 2`. **Settings** also shows a warning bar on the Models tab while exactly one model is enrolled, with an **Add a second model** action. 18 new strings translated in all 98 catalogs. Unit tests + real-GTK E2E (`UH_REAL_GTK=1`) cover both passes and both skip paths.
*   **🔔 New: native desktop notifications for every face-auth attempt** (`notify.py`, `org.freedesktop.Notifications`): one card **updated in place** — "Face authentication · sudo — 👀 Looking for your face…" stays on screen for the **whole scan** (critical urgency: GNOME never auto-hides it), then becomes **Face recognized · sudo — ✓ Full Name · 1.3 s** (removed after `success_linger` = 3 s) or **Face not recognized — ✗ No match in 8 s · 💡 add a model in this light** (stays in the list). Titles name the requester (`sudo`, `su`, `authorization`, `screen unlock`) because the PAM module now forwards the PAM service to `compare.py`. `[notifications] details = true` adds model, certainty/threshold, frames, dark frames, camera, pid, timeout, load. The notification id is persisted per user so consecutive attempts reuse the **same card**; a detached helper closes the success card and acts as a watchdog for a helper that died mid-scan. Layout follows GNOME Shell 50's rules read from its source (one-line body, `<b>` only; no `desktop-entry`/`transient`; low urgency never banners). Because `compare.py` runs as root with an empty environment, cards are sent on the **target user's** session bus by dropping to that uid before exec'ing the absolute `/usr/bin/gdbus`; greeter logins, missing gdbus, or any error silently disable it — never the auth result. Verified live on GNOME 50 / Wayland. Strings translated in all 98 catalogs; `libglib2.0-bin` / `glib2` added to core runtime deps.
*   **📦 Dependency updates (product + CI), all to latest final releases**: `dlib` `19.24.9` → **`20.0.1`** (postinst pip build, `scripts/package-configure.sh`) and `19.24.6` → **`20.0.1`** (snap payload) — verified to build from source on Ubuntu 26.04 / Python 3.14 with the full API intact; the `CMAKE_POLICY_VERSION_MINIMUM` shim is kept as a harmless no-op for `UH_DLIB_PIP_SPEC` overrides to 19.24.x. `inih` Meson wrap `r53` → **`r62`** (wrapdb `r62-1`, verified via `--force-fallback-for=inih`). Flatpak runtime/SDK `25.08` → **`26.08`** (manifest + release-builder image). Dockerfile frontend `docker/dockerfile:1.26.0` → **`1.27.0`** (all 16 Dockerfiles). CI `coverage` `7.15.4` → **`7.16.1`**. `archlinux:base-devel` snapshot `20260816` → **`20260906`**. GitHub Actions: `docker/setup-qemu-action` `v4.2.0` → **`v4.3.0`**, `softprops/action-gh-release` `v3.0.2` → **`v3.0.3`**. Already current and left as-is: `actions/checkout` v7.0.1, `actions/upload-artifact` v7.0.1, `actions/download-artifact` v8.0.1, `crazy-max/ghaction-github-runtime` v4.0.0, `docker/setup-buildx-action` v4.3.0, appimagetool 1.9.1, pytest 9.1.1, pytest-cov 7.1.0, keyboard 0.13.5, snapcraft rev 18514 (9.0.1), `ubuntu:26.04`, `opensuse/leap:16.0`. **Deliberately not bumped**: `fedora:44` (Fedora 45 is still `pending` in Bodhi, not final) and snap base `core24` (a `core26` migration changes the build host/plugins and can't be validated locally — tracked as follow-up).
*   **🧪 Verified**: new regression test `test_auth_overlay_uses_absolute_gtk_path` in `tests/test_compare_cleanup.py` (asserts the constant and that no bare-name `Popen` remains); `pam_ubuntu_hello.so` builds clean from a fresh Meson configure; Meson C++ tests (`pam-face-skip`, `pam-aes-gcm-uh1`) pass; `tests/test_compare_cleanup.py`, `tests/test_compare_timeout.py`, and `scripts/no-suppressions-lint.py` pass.

New config section `[notifications]` (`enabled`, `details`); the wizard's face-scan slide now has a second pass. Packaging: fresh installs now compile `dlib` 20.x during package configure (existing hosts with the pip marker are not rebuilt); Flatpak bundle targets the 26.08 runtime.

---

## 🖥️ Desktop / Greeter / Wallet Compatibility (unchanged)

Face auth via PAM (`common-auth`) remains desktop-agnostic with **GDM, SDDM, LightDM**:

| Desktop | Face auth | Theme follow | Session wallet auto-unlock |
|---------|-----------|--------------|----------------------------|
| **GNOME / Ubuntu** | Yes | Yes | GNOME Keyring (`pam_gnome_keyring`) |
| **KDE Plasma** | Yes | Yes | **KWallet** (`pam_kwallet5`) |
| **XFCE** | Yes | Yes | GNOME Keyring when present |
| **Cinnamon** | Yes | Yes | GNOME Keyring when present |
| **MATE** | Yes | Yes | GNOME Keyring when present |
| **Budgie** | Yes | Yes | GNOME Keyring when present |
| **LXQt** | Yes | Yes | GNOME Keyring when present |

CI compat cells (parallel, Ubuntu 26.04): `baseline`, `gnome`, `kde`, `xfce`, `cinnamon`, `mate`, `budgie`, `lxqt`.

---

## 📥 Installation

Download assets from the [GitHub Release](https://github.com/ventura8/ubuntu-hello/releases/tag/v1.1.7) page. Verify against `SHA256SUMS` before installing.

### Method 1: Quick Install (from release tag)
Download and review [`install.sh`](https://github.com/ventura8/ubuntu-hello/blob/v1.1.7/install.sh) from the **v1.1.7** tag, then run `bash install.sh` locally.

### Method 2: PPA (Ubuntu / Linux Mint)
```bash
sudo add-apt-repository ppa:ventura8/ubuntu-hello
sudo apt update
sudo apt install ubuntu-hello
```

### Method 3: Debian `.deb` (amd64 + arm64)
```bash
sudo apt install ./ubuntu-hello_1.1.7-1ppa1_$(dpkg --print-architecture).deb ./ubuntu-hello-gtk_1.1.7-1ppa1_$(dpkg --print-architecture).deb
```

### Method 4: Fedora RPM (amd64 + arm64)
```bash
sudo dnf install ./ubuntu-hello-1.1.7-1.fc44.$(uname -m).rpm ./ubuntu-hello-gtk-1.1.7-1.fc44.$(uname -m).rpm
```

### Method 5: openSUSE RPM (amd64 + arm64)
```bash
sudo zypper install ./ubuntu-hello-1.1.7-1.lp160.$(uname -m).rpm ./ubuntu-hello-gtk-1.1.7-1.lp160.$(uname -m).rpm
```

### Method 6: Arch Linux (amd64 only)
```bash
sudo pacman -U ubuntu-hello-1.1.7-1-x86_64.pkg.tar.zst ubuntu-hello-gtk-1.1.7-1-x86_64.pkg.tar.zst
```

### Method 7: Snap (classic, amd64 only)
```bash
sudo snap install --dangerous --classic ubuntu-hello_1.1.7_amd64.snap
sudo snap run ubuntu-hello.host-install
```

### Method 8: AppImage (installer, amd64 + arm64)
```bash
chmod +x Ubuntu-Hello-1.1.7-$(uname -m).AppImage
./Ubuntu-Hello-1.1.7-$(uname -m).AppImage --install
```

### Method 9: Flatpak (installer, amd64 only)
```bash
flatpak install --user ./com.github.ventura8.UbuntuHello-1.1.7-x86_64.flatpak && \
    flatpak run --command=ubuntu-hello-host-install com.github.ventura8.UbuntuHello
```
Approve the polkit prompt on the host-install step to install PAM and system files.

---

## 📋 Changelog (v1.1.7)

*   **Release**: Version **`1.1.7`** / **`v1.1.7`** from repo-root **`VERSION`**; `debian/changelog` top entry added (`urgency=high`).
*   **Security (PAM)**: `pam_ubuntu_hello.so` spawns `compare.py` with `posix_spawn()` and an explicit minimal environment (`PATH=/usr/sbin:/usr/bin:/sbin:/bin` only) instead of a `NULL` `envp`, and runs the interpreter as `python3 -E -s`. The caller's `PATH` / `PYTHON*` / `LD_*` variables can never reach the root helper. `-I` intentionally not used (would break sibling imports).
*   **Security (compare)**: auth overlay started via absolute `GTK_BIN_PATH = /usr/bin/ubuntu-hello-gtk` instead of a bare-name `PATH` lookup, matching `BUSCTL_PATH`.
*   **Assessment**: the scanner's "fully inherited environment" premise was incorrect — glibc's `posix_spawnp(NULL envp)` yields an empty environment and the default `/bin:/usr/bin` search path, so the described `~/.local/bin` privesc did not work before this release. The change removes the reliance on that unspecified behaviour.
*   **Docs**: `docs/SECURITY.md` ("No Inherited Environment / PATH"), `docs/architecture/README.md` §3.1, and `AGENTS.md` (sequence diagram + spawning rule) updated.
*   **Fix (keyring/TPM)**: polkit ≥ 126 helper sandbox blocked `/dev/tpmrm0` and made `tpm-keys/` read-only → unseal failed on every `pkexec`; drop-in now allows `char-tpm`, `/dev/tpm0`, `ReadWritePaths=/etc/ubuntu-hello/tpm-keys` (all three writers). Verified live.
*   **Feature (wizard)**: two-pass enrollment on slide 4 with why/how copy for different lighting; skip either pass but not both; non-fatal second-scan failure; labels `Setup lighting 1/2`; Settings Models tab reminder while only one model exists; unit + real-GTK E2E tests.
*   **Feature (notifications)**: `notify.py` — one desktop card per attempt, updated in place, with certainty/threshold/frames/fps/dark/elapsed and a near-miss lighting tip; `[notifications] enabled/details`; sent on the user's session bus after dropping root; best-effort; `tests/test_notify.py` (23 tests) + compare hooks.
*   **Dependencies**: `dlib` → 20.0.1 (postinst pip + snap), `inih` wrap → r62, Flatpak runtime → 26.08, Dockerfile frontend → 1.27.0, CI `coverage` → 7.16.1, `archlinux:base-devel` → 20260906, `docker/setup-qemu-action` → v4.3.0, `softprops/action-gh-release` → v3.0.3. Not bumped: `fedora:44` (45 not final), snap `core24` (core26 migration is a follow-up).
*   **Tests**: `test_auth_overlay_uses_absolute_gtk_path` added to `tests/test_compare_cleanup.py`; PAM module builds clean; Meson C++ tests and compare/lint pytest targets pass.
